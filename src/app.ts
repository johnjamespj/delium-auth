/**
 * Delium Auth Service: A service that manages users authentication.
 * 
 *      This service creates a single users, edit user, create access tokens, revoke access
 * tokens, edit user and delete user. This services includes a a cote publisher and responder
 * that can be used to use the service. For the data storage, a "repo" abstraction is made so
 * that any type of data storage can be used, with mongodb as the default data store.
 * 
 *  # Create a user
 *  # Edit a user
 *  # Recover account
 *  # Create access tokens
 *  # Revoke access tokens
 *  # Edit a user
 *  # Delete a user
 */